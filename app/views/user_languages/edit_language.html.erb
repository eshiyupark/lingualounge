<div class="container">
<h2>Languages:</h2>
  <%= form_tag update_language_url, method: :patch do %>
    <% @user_languages.each do |user_language| %>
      <%= check_box_tag 'user_language_ids[]', user_language.id %>
      <%= user_language.language.name.capitalize %>
    <% end %>

    <%= submit_tag 'Take me to Lounge!', class: "btn-orange" %>
  <% end %>
   <%= link_to "Back to Home", root_path, class: "btn-orange" %>
</div>

      <%#= form_tag("/sessions", method: "post", class: "w-100") do %>
      <%#= submit_tag("Enter Lounge", class: "btn-orange") %>
      <%# end %>
